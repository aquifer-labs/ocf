{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aquifer-labs.github.io/ocf/schema/manifest.schema.json",
  "title": "OCF Manifest",
  "type": "object",
  "required": ["ocf_version", "created", "unit_source"],
  "properties": {
    "ocf_version": { "type": "string" },
    "agent_id": { "type": "string" },
    "created": { "type": "string", "format": "date-time" },
    "default_namespace": {
      "type": "string",
      "minLength": 1,
      "default": "shared",
      "description": "Default namespace/project for snapshot entries that omit namespace. If absent, the effective default is shared."
    },
    "unit_source": {
      "type": "string",
      "description": "inline | pam | amp | oamp | artesian | files | ..."
    },
    "unit_refs": { "type": "array", "items": { "type": "string" } },
    "session": {
      "type": "object",
      "description": "Optional work-session identity for cross-agent handoff. A resumable bundle is addressed by (user_id, session_id, task_id); agent_id changes on handoff and is recorded in handed_off_from.",
      "properties": {
        "session_id": { "type": "string", "description": "stable id for this work session" },
        "task_id": { "type": "string", "description": "the task being worked" },
        "user_id": { "type": "string", "description": "owning user / tenant" },
        "handed_off_from": { "type": "string", "description": "agent_id of the runtime that last committed this bundle" }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
